CREATE OR REPLACE FUNCTION count_employees(deptid FITZ.EMPLOYEES.DEPARTMENT_ID%TYPE)
RETURN NUMBER 
IS
emp_count NUMBER(6);
BEGIN
  SELECT COUNT (EMPLOYEE_ID) INTO emp_count
  FROM FITZ.EMPLOYEES
  WHERE DEPARTMENT_ID = deptid;
  RETURN emp_count;
END;